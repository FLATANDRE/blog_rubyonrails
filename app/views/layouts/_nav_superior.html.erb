<div class="nav-scroller py-1 mb-2">
    <nav class="nav d-flex justify-content-between">
        <% @categories.each do |category| %>
            <a class="p-2 link-secondary" href="#"><%= category.alias %></a>    
        <% end %>
    </nav>
</div>